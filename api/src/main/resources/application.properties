# Server
server.port=8080

# Application
spring.application.name=portfolio-api

# YouTube Data API v3 (for /api/youtube/videos). Set YOUTUBE_API_KEY env var or use same key as frontend.
youtube.api-key=
youtube.channel-handle=pavlosrev

# CORS: allow my-portfolio frontend (Vite dev server and production)
# Adjust origins in production as needed
cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
